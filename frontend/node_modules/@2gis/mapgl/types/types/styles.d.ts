/**
 * @deprecated
 */
export type InterpolateExpression<T extends number | string> = [
    'interpolate',
    [
        'linear'
    ] | ['exponential', number],
    [
        'zoom'
    ],
    ...T[]
];
export type TypeExpressionName = 'to-boolean' | 'to-color' | 'literal';
export type ExtractorExpressionName = 'get' | 'sourceAttr' | 'featureState' | 'global';
export type LogicalExpressionName = '!' | '==' | '!=' | '>' | '>=' | '<' | '<=' | 'match' | 'all' | 'any' | 'in';
export type MathExpressionName = '^' | 'log10';
export type InterpolateExpressionName = 'interpolate' | 'step';
export type ExpressionName = TypeExpressionName | ExtractorExpressionName | LogicalExpressionName | MathExpressionName | InterpolateExpressionName | 'pattern';
/**
 * Type representing the style layer property expressions.
 *
 * For detailed information about expression compositing, exceptions,
 * and specific property requirements for different layer types,
 * refer to the MapGL JS API documentation.
 */
export type Expression = [ExpressionName, ...any[]];
/**
 * Extra margins for objects.
 */
interface LabelingMargin {
    /**
     * Top and bottom margin in pixels.
     */
    topBottom?: number;
    /**
     * Left and right margin in pixels.
     */
    leftRight?: number;
}
interface StyleLayerBase {
    /**
     * Layer identifier.
     */
    id: string;
    /**
     * Filter that selects objects for the layer.
     */
    filter?: boolean | Expression;
    /**
     * Minimum map zoom level for the layer.
     */
    minzoom?: number;
    /**
     * Maximum map zoom level for the layer.
     */
    maxzoom?: number;
}
/**
 * Polygon style layer.
 */
export interface PolygonStyleLayer extends StyleLayerBase {
    /**
     * Layer type.
     */
    type: 'polygon';
    /**
     * Set of style properties for the layer.
     */
    style: {
        /**
         * Fill color.
         */
        color?: string | Expression;
        /**
         * Stroke color.
         */
        strokeColor?: string | Expression;
        /**
         * Stroke width in pixels.
         */
        strokeWidth?: number | Expression;
        /**
         * Whether the layer objects are displayed on the map. `visible` - displayed, `none` - not displayed.
         */
        visibility?: string;
    };
}
/**
 * Line style layer.
 */
export interface LineStyleLayer extends StyleLayerBase {
    /**
     * Layer type.
     */
    type: 'line';
    /**
     * Set of style properties for the layer.
     */
    style: {
        /**
         * Line color.
         */
        color?: string | Expression;
        /**
         * Line width in pixels.
         */
        width?: number | Expression;
        /**
         * Line pattern.
         */
        pattern?: Expression;
        /**
         * Whether the layer objects are displayed on the map. `visible` - displayed, `none` - not displayed.
         */
        visibility?: string;
    };
}
/**
 * Dashed line style layer.
 */
export interface DashedLineStyleLayer extends StyleLayerBase {
    /**
     * Layer type.
     */
    type: 'dashedLine';
    /**
     * Set of style properties for the layer.
     */
    style: {
        /**
         * Color of line dashes.
         */
        color?: string | Expression;
        /**
         * Line width in pixels.
         */
        width?: number | Expression;
        /**
         * Dash length in pixels.
         */
        dashLength?: number | Expression;
        /**
         * Gap length in pixels.
         */
        gapLength?: number | Expression;
        /**
         * Color of line gaps.
         */
        gapColor?: string | Expression;
        /**
         * Whether the layer objects are displayed on the map. `visible` - displayed, `none` - not displayed.
         */
        visibility?: string;
    };
}
/**
 * Point style layer.
 */
export interface PointStyleLayer extends StyleLayerBase {
    /**
     * Layer type.
     */
    type: 'point';
    /**
     * Set of style properties for the layer.
     */
    style: {
        /**
         * Icon name. If not specified, icon is not displayed.
         */
        iconImage?: string | Expression;
        /**
         * Icon width in pixels.
         */
        iconWidth?: number | Expression;
        /**
         * Icon anchor position.
         */
        iconAnchor?: Expression;
        /**
         * Offset of the `iconAnchor` point.
         */
        iconOffset?: Expression;
        /**
         * Label text.
         */
        textField?: string | Expression;
        /**
         * Label font. If not specified, label is not displayed.
         */
        textFont?: string | Expression;
        /**
         * Label color.
         */
        textColor?: string | Expression;
        /**
         * Label font size in pixels.
         */
        textFontSize?: number | Expression;
        /**
         * Label line spacing in relative units (em).
         */
        textLineHeight?: number;
        /**
         * Label letter spacing in relative units (em).
         */
        textLetterSpacing?: number;
        /**
         * Label location relative to the icon. If the icon is not specified, the parameter is not used.
         */
        textPlacement?: string | Expression;
        /**
         * Label offset from the icon in pixels.
         */
        textOffset?: number | Expression;
        /**
         * Label outline color.
         */
        textHaloColor?: string | Expression;
        /**
         * Label outline width in pixels.
         */
        textHaloWidth?: number;
        /**
         * Line length for word wrapping (number of characters). If the value is greater, the next word is wrapped to the next line.
         */
        textMaxLengthPerLine?: number;
        /**
         * If `true`, the object is not a part of any labeling group and is always displayed. The `iconLabelingGroup` and `textLabelingGroup` properties are ignored.
         */
        allowOverlap?: boolean;
        /**
         * Labeling group for the icon.
         */
        iconLabelingGroup?: string;
        /**
         * Extra icon margins in a labeling group.
         */
        iconLabelingMargin?: LabelingMargin;
        /**
         * Icon priority in a labeling group. The higher the value, the higher the priority.
         */
        iconPriority?: number;
        /**
         * Labeling group for the label.
         */
        textLabelingGroup?: string;
        /**
         * Extra label margins in a labeling group.
         */
        textLabelingMargin?: LabelingMargin;
        /**
         * Label priority in a labeling group. The higher the value, the higher the priority. The value cannot exceed the `iconPriority` value. The label is hidden if its icon overlaps it.
         */
        textPriority?: number;
        /**
         * Whether the layer objects are displayed on the map. `visible` - displayed, `none` - not displayed.
         */
        visibility?: string;
    };
}
/**
 * Raster style layer.
 */
export interface RasterStyleLayer extends StyleLayerBase {
    /**
     * Layer type.
     */
    type: 'raster';
    /**
     * Set of style properties for the layer.
     */
    style: {
        /**
         * Image opacity. `0` - image is fully transparent (invisible), `1` - image is fully filled.
         */
        opacity?: number | Expression;
        /**
         * Whether the layer objects are displayed on the map. `visible` - displayed, `none` - not displayed.
         */
        visibility?: string;
    };
}
/**
 * Heatmap style layer.
 */
export interface HeatmapStyleLayer extends StyleLayerBase {
    /**
     * Layer type.
     */
    type: 'heatmap';
    /**
     * Set of style properties for the layer.
     */
    style: {
        /**
         * Color of each pixel depending on the `intensity` value.
         */
        color?: Expression;
        /**
         * Radius of heatmap points in pixels. The greater the value, the smoother the heatmap.
         */
        radius?: number | Expression;
        /**
         * Opacity of the entire heatmap layer. `0` - layer is fully transparent (invisible), `1` - layer is fully filled.
         */
        opacity?: number | Expression;
        /**
         * Heatmap kernel multiplier. The higher the value, the higher the color intensity. Applied to all heatmap points, unlike the `weight` parameter.
         */
        intensity?: number | Expression;
        /**
         * How much a single point contributes to a heatmap. For example, `10` is equivalent to 10 points with a weight of `1` in the same location.
         */
        weight?: number | Expression;
        /**
         * Heatmap texture divider. The higher the value, the lower the heatmap quality, but the faster the speed of heatmap rendering.
         */
        downscale?: number;
        /**
         * Whether the layer objects are displayed on the map. `visible` - displayed, `none` - not displayed.
         */
        visibility?: string;
    };
}
/**
 * Model style layer.
 */
export interface ModelStyleLayer extends StyleLayerBase {
    /**
     * Layer type.
     */
    type: 'model';
    /**
     * Set of style properties for the layer.
     */
    style: {
        /**
         * URL to the model file.
         */
        modelSrc?: string | Expression;
        /**
         * Color that blends with the model. By default, the color is blended with the model's texture using multiplication.
         */
        color?: string | Expression;
        /**
         * Scale factor for the model.
         */
        scale?: number | Expression;
        /**
         * Rotation of the model around each axis in degrees.
         */
        rotation?: number | Expression;
        /**
         * Offset for the model along each axis in meters.
         */
        offset?: number | Expression;
        /**
         * If the value is `true`, the model ignores global lighting settings.
         */
        ignoreGlobalLighting?: boolean;
        /**
         * Model animation index or name to play.
         */
        playAnimation?: number | string | Expression;
        /**
         * List of map object IDs to hide when displaying the model.
         */
        linkedIds?: string | Expression;
        /**
         * Texture coordinates index for the color texture.
         */
        colorTextureUvIndex?: number;
        /**
         * Percentage of visible model instances. `0` - no instances are visible, `1` - all instances are visible, `0.5` - half of the instances are visible.
         */
        showRatio?: number | Expression;
        /**
         * Whether the layer objects are displayed on the map. `visible` - displayed, `none` - not displayed.
         */
        visibility?: string;
    };
}
/**
 * Polygon extrusion style layer.
 */
export interface PolygonExtrusionStyleLayer extends StyleLayerBase {
    /**
     * Layer type.
     */
    type: 'polygonExtrusion';
    /**
     * Set of style properties for the layer.
     */
    style: {
        /**
         * Fill color of the top face of the polygon extrusion.
         */
        topColor?: string | Expression;
        /**
         * Fill color of the side faces of the polygon extrusion.
         */
        sideColor?: string | Expression;
        /**
         * Stroke color.
         */
        strokeColor?: string | Expression;
        /**
         * Stroke width in pixels.
         */
        strokeWidth?: number | Expression;
        /**
         * Side stroke color.
         */
        sideStrokeColor?: string | Expression;
        /**
         * Height of the polygon extrusion in meters.
         */
        height?: number | Expression;
        /**
         * Whether the layer objects are displayed on the map. `visible` - displayed, `none` - not displayed.
         */
        visibility?: string;
    };
}
/**
 * Line extrusion style layer.
 */
export interface LineExtrusionStyleLayer extends StyleLayerBase {
    /**
     * Layer type.
     */
    type: 'lineExtrusion';
    /**
     * Set of style properties for the layer.
     */
    style: {
        /**
         * Fill color of the side faces of the line extrusion.
         */
        sideColor?: string | Expression;
        /**
         * Stroke color.
         */
        strokeColor?: string | Expression;
        /**
         * Stroke width in pixels.
         */
        strokeWidth?: number | Expression;
        /**
         * Side stroke color.
         */
        sideStrokeColor?: string | Expression;
        /**
         * Height of the line extrusion in centimeters.
         */
        height?: number | Expression;
        /**
         * Whether the layer objects are displayed on the map. `visible` - displayed, `none` - not displayed.
         */
        visibility?: string;
    };
}
/**
 * 3D polygon style layer.
 */
export interface Polygon3dStyleLayer extends StyleLayerBase {
    /**
     * Layer type.
     */
    type: 'polygon3d';
    /**
     * Set of style properties for the layer.
     */
    style: {
        /**
         * Fill color.
         */
        color?: string | Expression;
        /**
         * Name of the texture from the icon collection. If the value is not specified, the texture is not displayed on the polygon, and the polygon is filled with the specified or default color.
         */
        textureImage?: string | Expression;
        /**
         * Size of the texture cell of which the overall texture pattern consists. If one number is specified, its value is applied to both width and height of the image, forming a square. To make a rectangle, specify an array of two elements, where the first element is width and the second is height.
         */
        textureSize?: number | Expression;
        /**
         * Texture opacity.
         */
        textureOpacity?: number | Expression;
        /**
         * The elevation height of a polygon above ground level in meters.
         */
        elevation?: number | Expression;
        /**
         * Whether the layer objects are displayed on the map. `visible` - displayed, `none` - not displayed.
         */
        visibility?: string;
    };
}
/**
 * Metric point style layer.
 */
export interface MetricPointStyleLayer extends StyleLayerBase {
    /**
     * Layer type.
     */
    type: 'metricPoint';
    /**
     * Set of style properties for the layer.
     */
    style: {
        /**
         * Icon name. If not specified, the layer object is not displayed.
         */
        iconImage?: string | Expression;
        /**
         * Color tinting of `iconImage`. Color components are multiplied by the image color of `iconImage`.
         */
        color?: string | Expression;
        /**
         * Rotation of the layer object relative to the zero direction (north). The rotation center is the center of the layer object rectangle. Rotation is counterclockwise in degrees.
         */
        rotation?: number | Expression;
        /**
         * Width of the layer object in meters.
         */
        width?: number | Expression;
        /**
         * Height of the layer object in meters.
         */
        height?: number | Expression;
        /**
         * Whether the layer objects are displayed on the map. `visible` - displayed, `none` - not displayed.
         */
        visibility?: string;
    };
}
/**
 * Label line style layer.
 */
export interface LabelLineStyleLayer extends StyleLayerBase {
    /**
     * Layer type.
     */
    type: 'labelLine';
    /**
     * Set of style properties for the layer.
     */
    style: {
        /**
         * Text content for the label.
         */
        textField?: string | Expression;
        /**
         * Text font.
         */
        textFont?: string;
        /**
         * Text color.
         */
        textColor?: string | Expression;
        /**
         * Font size in pixels.
         */
        textFontSize?: number | Expression;
        /**
         * Letter spacing in relative units (em).
         */
        textLetterSpacing?: number;
        /**
         * Text outline color.
         */
        textHaloColor?: string | Expression;
        /**
         * Text outline width in pixels.
         */
        textHaloWidth?: number;
        /**
         * Labeling group for the layer.
         */
        labelingGroup?: string;
        /**
         * Label priority for labeling. The higher the value, the higher the priority.
         */
        textPriority?: number;
        /**
         * Additional side margins in pixels for the label during labeling.
         */
        textLabelingSideMargin?: number;
        /**
         * Distance in pixels between label duplicates along the line.
         */
        textDuplicationSpacing?: number | Expression;
        /**
         * Offset in pixels from line edges for label placement.
         */
        lineEndingOffsets?: number;
        /**
         * Whether the layer objects are displayed on the map. `visible` - displayed, `none` - not displayed.
         */
        visibility?: string;
    };
}
/**
 * Group layer.
 */
export interface GroupStyleLayer {
    /**
     * Layer type.
     */
    type: 'group';
    /**
     * Layer identifier.
     */
    id: string;
    /**
     * Array of layers that the group contains. The array must not contain other layers with the `group` type, because nested groups are not supported.
     */
    layers: Array<Exclude<Layer, GroupStyleLayer>>;
    /**
     * Defines the drawing order of objects on the map. Objects are grouped by the first expression. Within the resulting groups, they are regrouped by the next one, and so on.
     */
    orderBy?: Expression[];
}
/**
 * For more information on how to use style layers,
 * refer to the MapGL JS API documentation.
 */
export type Layer = PolygonStyleLayer | LineStyleLayer | DashedLineStyleLayer | PointStyleLayer | RasterStyleLayer | HeatmapStyleLayer | ModelStyleLayer | PolygonExtrusionStyleLayer | LineExtrusionStyleLayer | Polygon3dStyleLayer | MetricPointStyleLayer | LabelLineStyleLayer | GroupStyleLayer;
/**
 * Stub type representing the style.
 *
 * Real style types will appear a little bit later.
 */
export interface Style extends Record<string, any> {
}
/**
 * Style options - paths to icons and fonts.
 */
export interface StyleOptions {
    /**
     * The url to the styles.
     */
    stylePath?: string;
    /**
     * The url to the icons.
     */
    iconsPath: string;
    /**
     * The url to the fonts.
     */
    fontsPath: string;
    /**
     * The url to the models.
     */
    modelsPath: string;
}
/**
 * Config of an original user icon. Only SVG images are supported.
 */
export interface StyleIconConfig {
    /**
     * URL path to an icon:
     *  - absolute: `//external.domain/some_path/some_icon.svg`
     *  - template: `//{appHost}/some_path/some_icon.svg`
     */
    url: string;
}
export {};
